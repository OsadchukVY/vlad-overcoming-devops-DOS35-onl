---

- name: "Install patroni"
  become: true
  user: vboxuser
  become_user: root
  hosts: patroni
  tasks:
    - name: "Install postgres"
      ansible.builtin.apt:
        name: 
          - "postgres-16"
          - "patroni"
        state: "present"
        update_cache: yes

